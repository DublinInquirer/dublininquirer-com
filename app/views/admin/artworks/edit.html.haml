- content_for :subheader_detail do
  %div
    .breadcrumbs
      = link_to 'Article', [:admin, @article]
      = link_to 'Artwork', [:edit, :admin, @artwork]

    %nav
      = link_to "Cancel without saving", [:admin, @article], class: 'button -cancel'
      = link_to "Save", [:admin, :articles], class: 'button', data: {action: "form#submitForm"}

= render partial: 'admin/artworks/form', locals: {artwork: @artwork}