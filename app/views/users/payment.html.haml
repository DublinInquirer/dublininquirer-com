- content_for :breadcrumbs do
  = link_to "Account", :user
  .current Update your payment method

%header.header.-modal
  %h1.heading Update your payment method

  - if current_user.delinquent?
    .text.-article
      :markdown
        We're having trouble charging your account. Please enter your credit or debit card details below to avoid an interruption in your subscription.

.box.-tight
  = form_for @user, url: [:payment, :user], method: :put, html: {class: 'subscription-form', data: {'fonts-url': asset_url('fonts.css')}} do |f|
    .error{style: 'display:none;'}
      .message &nbsp; |f|

    .box-form
      .field
        %label Card number
        #stripe-form_card-number.field.empty

      .twofer
        .field
          %label Expiration date
          #stripe-form_card-expiry.field.empty
        .field
          %label Security code
          #stripe-form_card-cvc.field.empty

      %nav.actions
        %p.status
        = f.submit 'Save changes', class: 'button -standard'